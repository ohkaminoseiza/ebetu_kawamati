# 条丁目スタイル サイト利用手順書

> 本手順書は「条丁目スタイル」サイトのコンテンツを編集・更新するための手引きです。

---

## 目次

1. [サイトの全体構成](#1-サイトの全体構成)
2. [ファイル一覧と役割](#2-ファイル一覧と役割)
3. [プロジェクトの編集（最も頻繁な操作）](#3-プロジェクトの編集)
4. [個別事業（サブプロジェクト）の編集](#4-個別事業サブプロジェクトの編集)
5. [カテゴリの編集](#5-カテゴリの編集)
6. [画像の追加・変更](#6-画像の追加変更)
7. [ホーム画面のお知らせ文の変更](#7-ホーム画面のお知らせ文の変更)
8. [公開（デプロイ）手順](#8-公開デプロイ手順)
9. [記述ルールと注意事項](#9-記述ルールと注意事項)

---

## 1. サイトの全体構成

```
ホーム (index.html)
├── カテゴリ一覧（自動表示）
├── プロジェクト一覧カード（自動表示）
└── お知らせ＆掲示板

プロジェクト一覧 (projects.html)
└── カテゴリごとのプロジェクトリスト（自動表示）

カテゴリ詳細ページ（例: ekishuhen.html）
└── プロジェクト詳細カード（自動表示）
    ├── 説明文
    ├── 個別事業（サブプロジェクト）※ある場合のみ
    └── コメント欄
```

> [!IMPORTANT]
> サイトの内容（プロジェクト名、説明文、ステータス等）は **ほぼすべて `data.js` の1ファイルに集約**されています。通常の編集作業では、このファイルだけを修正すればOKです。

---

## 2. ファイル一覧と役割

| ファイル名 | 役割 | 編集する場面 |
|-----------|------|------------|
| **`data.js`** | 全プロジェクト・カテゴリのデータ定義 | **コンテンツ編集時（最も頻繁）** |
| `main.js` | 画面の描画ロジック | 通常は変更不要 |
| `style.css` | デザイン・見た目 | デザイン変更時のみ |
| `index.html` | ホームページの外枠 | 通常は変更不要 |
| `projects.html` | プロジェクト一覧の外枠 | 通常は変更不要 |
| `ekishuhen.html` 等 | 各カテゴリページの外枠（8カテゴリ分） | 新カテゴリ追加時のみ |
| `firebase-config.js` | コメント機能の設定 | 変更不要 |
| `comments.js` | コメント機能のロジック | 通常は変更不要 |
| `images/` フォルダ | プロジェクト用の画像 | 画像変更・追加時 |

---

## 3. プロジェクトの編集

### 編集するファイル: `data.js`

### 3-1. 既存プロジェクトの内容を変更する

`data.js` の `SITE_PROJECTS` 配列の中から、変更したいプロジェクトを探して値を書き換えます。

```js
// data.js 内のプロジェクト例（江別駅周辺）
{
    id: 'ebetsu-eki',                 // ID（変更しない）
    categoryId: 'ekishuhen',          // 所属カテゴリのID（変更しない）
    title: '江別駅周辺',               // ← プロジェクト名
    icon: '🚉',                       // ← 表示アイコン（絵文字）
    status: 'active',                 // ← ステータス（後述）
    statusLabel: '進行中',             // ← ステータスの表示名
    image: 'images/ebetsu-eki.jpg',   // ← 画像ファイルパス
    summary: '商業機能と空き店舗...',   // ← 一覧カードに表示される短い説明
    detail: '江別駅周辺において...',    // ← 詳細ページに表示される長い説明
},
```

#### 各フィールドの説明

| フィールド | 内容 | 例 |
|-----------|------|-----|
| `id` | 一意の識別子。**変更しないでください** | `'ebetsu-eki'` |
| `categoryId` | 所属カテゴリのID | `'ekishuhen'` |
| `title` | プロジェクト名 | `'江別駅周辺'` |
| `icon` | 表示用の絵文字（1つ） | `'🚉'` |
| `status` | ステータスコード | `'active'` または `'planning'` |
| `statusLabel` | 画面に表示されるステータス名 | `'進行中'` または `'企画中'` |
| `image` | 画像のファイルパス | `'images/ebetsu-eki.jpg'` |
| `imageClass` | 画像がない場合の代替色 | `'img-community'` |
| `summary` | カード上の短い概要文 | 1〜2文程度 |
| `detail` | 詳細ページの説明文 | 3〜5文程度 |

#### ステータスの種類

| `status` の値 | `statusLabel` | 色 |
|--------------|---------------|-----|
| `'active'` | `'進行中'` | 緑色 |
| `'planning'` | `'企画中'` | 黄色 |

### 3-2. 新しいプロジェクトを追加する

`SITE_PROJECTS` 配列の末尾（最後の `}` の後ろ）に、新しいブロックを追加します。

**手順：**

1. `data.js` を開く
2. 既存のプロジェクトのブロック（`{ }` で囲まれた部分）をコピーする
3. `SITE_PROJECTS` 配列の末尾に貼り付ける
4. 各フィールドの値を書き換える

```js
// 追加例：「大川通商店街」を新規追加
{
    id: 'ookawa-dori',              // ← 他と重複しないIDを付ける
    categoryId: 'ekishuhen',        // ← 所属カテゴリのID
    title: '大川通商店街',
    icon: '🏪',
    status: 'planning',
    statusLabel: '企画中',
    image: 'images/ookawa-dori.jpg', // ← 画像を用意する（後述）
    summary: '大川通の商店街活性化に向けた取り組みを検討します。',
    detail: '大川通商店街の活性化に向け、歩行者空間の整備や地域イベントの開催を検討しています。'
},
```

> [!WARNING]
> - `id` は **サイト全体で重複してはいけません**
> - `categoryId` は `SITE_CATEGORIES` に存在するIDでなければなりません
> - 各ブロックの末尾に **カンマ `,`** を忘れないでください

### 3-3. プロジェクトを削除する

対象のプロジェクトのブロック全体（`{` から `},` まで）を削除します。

> [!CAUTION]
> プロジェクトを削除すると、そのプロジェクトに付けられたコメントは画面に表示されなくなります（Firebase上にデータは残ります）。

---

## 4. 個別事業（サブプロジェクト）の編集

### 編集するファイル: `data.js`

プロジェクトの中に複数の具体的な事業がある場合、`subProjects` フィールドを使って個別事業を追加できます。

### 4-1. 既存の個別事業を変更する

`data.js` の対象プロジェクト内にある `subProjects` 配列から、該当する事業を探して値を書き換えます。

```js
subProjects: [
    {
        id: 'akitenpo-katsuyo',            // ID（変更しない）
        title: '空き店舗活用プロジェクト',     // ← 事業名
        status: 'active',                   // ← 'active' or 'planning'
        statusLabel: '進行中',               // ← '進行中' or '企画中'
        description: '説明文をここに書く...',  // ← 事業の詳細説明
        progress: 40,                       // ← 進捗率（0〜100の数値）
        updatedAt: '2026-05-01'             // ← 最終更新日（YYYY-MM-DD形式）
    },
    // ... 他の個別事業
]
```

#### 各フィールドの説明

| フィールド | 内容 | 例 |
|-----------|------|-----|
| `id` | 一意の識別子 | `'akitenpo-katsuyo'` |
| `title` | 事業名 | `'空き店舗活用プロジェクト'` |
| `status` | ステータスコード | `'active'` または `'planning'` |
| `statusLabel` | ステータス表示名 | `'進行中'` または `'企画中'` |
| `description` | 詳しい説明文 | 自由に記述 |
| `progress` | 進捗率（数値） | `40`（= 40%）|
| `updatedAt` | 最終更新日 | `'2026-05-01'` |

### 4-2. 新しい個別事業を追加する

対象プロジェクトの `subProjects` 配列に、新しいブロックを追加します。

```js
// 追加例
{
    id: 'atarashii-jigyou',
    title: '新しい事業名',
    status: 'planning',
    statusLabel: '企画中',
    description: 'この事業の詳しい説明をここに記入します。',
    progress: 0,
    updatedAt: '2026-05-11'
}
```

### 4-3. まだ個別事業がないプロジェクトに初めて追加する場合

`detail` フィールドの行末に **カンマ `,`** を追加してから、`subProjects` ブロックを追記します。

**変更前：**
```js
{
    id: 'bousai',
    categoryId: 'ekishuhen',
    title: '防災ステーション',
    // ... 中略 ...
    detail: '石狩川と千歳川の合流点付近に...'    // ← カンマなし
},
```

**変更後：**
```js
{
    id: 'bousai',
    categoryId: 'ekishuhen',
    title: '防災ステーション',
    // ... 中略 ...
    detail: '石狩川と千歳川の合流点付近に...',   // ← カンマを追加！
    subProjects: [                               // ← 追加
        {
            id: 'bousai-tenji',
            title: '防災展示リニューアル',
            status: 'planning',
            statusLabel: '企画中',
            description: '展示内容の見直しと更新を検討しています。',
            progress: 10,
            updatedAt: '2026-05-11'
        }
    ]
},
```

> [!TIP]
> `subProjects` がないプロジェクトでは、個別事業セクションは自動的に非表示になります。すべてのプロジェクトに追加する必要はありません。

---

## 5. カテゴリの編集

### 編集するファイル: `data.js`（＋ 新規カテゴリの場合は HTMLファイルの追加）

### 5-1. カテゴリ名や説明文を変更する

`data.js` の `SITE_CATEGORIES` 配列の中から対象を探して書き換えます。

```js
const SITE_CATEGORIES = [
    {
        id: 'ekishuhen',                    // ID（変更しない）
        title: '駅周辺・大川通',              // ← カテゴリ名
        file: 'ekishuhen.html',             // ← 対応するHTMLファイル名
        lead: '駅前、大川通、防災拠点...'     // ← カテゴリの説明文
    },
    // ...
];
```

### 5-2. 新しいカテゴリを追加する

2つのファイルを操作する必要があります：

**① `data.js` にカテゴリを追加：**

```js
{
    id: 'new-category',
    title: '新しいカテゴリ名',
    file: 'new-category.html',
    lead: 'カテゴリの説明文をここに書きます。'
},
```

**② 対応するHTMLファイルを作成：**

既存のカテゴリページ（例: `ekishuhen.html`）をコピーし、以下の2箇所を変更します。

```html
<!-- ① <title>タグを変更 -->
<title>新しいカテゴリ名 | 条丁目スタイル</title>

<!-- ② data-category属性を変更（data.jsのidと一致させる） -->
<body data-page="category" data-category="new-category">
```

---

## 6. 画像の追加・変更

### 対象フォルダ: `images/`

### 6-1. プロジェクトに画像を設定する

1. 使用する画像ファイルを `images/` フォルダに配置する
2. `data.js` のプロジェクトで `image` フィールドにパスを記述する

```js
image: 'images/ファイル名.jpg',    // jpg, png に対応
```

### 6-2. 画像がない場合

画像を用意できない場合は、`image` の代わりに `imageClass` を使って背景色を表示できます。

```js
imageClass: 'img-community',    // 紫系グラデーション
```

**使用可能な `imageClass` 一覧：**

| クラス名 | 色合い |
|---------|--------|
| `img-forest` | 緑系（森） |
| `img-river` | 青系（川） |
| `img-forest2` | 深緑系 |
| `img-festival` | 黄緑系 |
| `img-house` | 茶系（建物） |
| `img-green` | 明るい緑系 |
| `img-community` | 紫系（交流） |
| `img-study` | 紺系（学問） |
| `img-migration` | 暖色系 |

### 6-3. 画像の推奨仕様

| 項目 | 推奨値 |
|------|--------|
| 形式 | JPG または PNG |
| 横幅 | 800px 以上 |
| アスペクト比 | 16:9 程度（横長） |
| ファイルサイズ | 500KB 以下を推奨 |

---

## 7. ホーム画面のお知らせ文の変更

### 編集するファイル: `main.js`

`main.js` 内の `renderHome` 関数にある `notice-section` 部分を探し、`<p>` タグ内のテキストを書き換えます。

```js
// main.js 内（約113〜117行目付近）
<div class="notice-section">
    <h2 class="notice-title">お知らせ&amp;掲示板</h2>
    <p>ここにお知らせ文を書きます。</p>
    <p>複数の段落を追加できます。</p>
</div>
```

---

## 8. 公開（デプロイ）手順

サイトは GitHub Pages で公開されています。変更を公開するには、以下の手順で Git にプッシュします。

```bash
# 1. 変更したファイルをステージングする
git add -A

# 2. コミットメッセージを書いてコミットする
git commit -m "江別駅周辺のサブプロジェクトを更新"

# 3. GitHub にプッシュする
git push
```

> [!NOTE]
> プッシュ後、GitHub Pages に反映されるまで **1〜2分程度** かかる場合があります。

---

## 9. 記述ルールと注意事項

### ✅ 守ってほしいルール

1. **文字列はシングルクォーテーション `'` で囲む**
   ```js
   title: '正しい書き方',     // ✅
   title: "これも動くが統一する", // ⚠️ 非推奨
   ```

2. **各ブロックの末尾にカンマ `,` を付ける**
   ```js
   { id: 'aaa', title: 'テスト' },   // ✅ カンマあり
   { id: 'bbb', title: 'テスト' }    // ⚠️ 配列の最後の要素は無くてもOK
   ```

3. **`id` はサイト全体で重複させない**
   - 英数字・ハイフンで構成する（日本語不可）
   - 例: `'ekimae-nigiwai'`, `'bousai-tenji'`

4. **`progress` は 0〜100 の数値を指定する**（パーセンテージ）
   ```js
   progress: 40,     // ✅ 数値（クォーテーションなし）
   progress: '40',   // ❌ 文字列にしない
   ```

### ⚠️ よくある間違い

| 症状 | 原因 | 対処 |
|------|------|------|
| ページが真っ白になる | `data.js` の文法エラー（カンマ忘れ等） | ブラウザの開発者ツール（F12）→ コンソールでエラーを確認 |
| 新しいプロジェクトが表示されない | `categoryId` が間違っている | `SITE_CATEGORIES` のIDと一致しているか確認 |
| 画像が表示されない | ファイルパスの間違いまたはファイル未配置 | `images/` にファイルがあるか、パスが正しいか確認 |
| 個別事業セクションが出ない | `subProjects` の前にカンマ `,` がない | `detail` の行末にカンマを追加する |

### 🔍 変更前の確認方法

変更内容を公開する前に、ローカルで確認できます：

1. 修正した `data.js` を保存する
2. `index.html` をブラウザで直接開く（ダブルクリック）
3. 対象のカテゴリページに移動して表示を確認する

---

## クイックリファレンス

| やりたいこと | 編集するファイル | 編集箇所 |
|-------------|----------------|----------|
| プロジェクト名・説明文の変更 | `data.js` | `SITE_PROJECTS` 内の対象プロジェクト |
| プロジェクトの追加 | `data.js` | `SITE_PROJECTS` 配列に追加 |
| ステータスの変更 | `data.js` | `status` と `statusLabel` を変更 |
| 個別事業の追加・変更 | `data.js` | 対象プロジェクトの `subProjects` |
| 進捗率の更新 | `data.js` | `subProjects` 内の `progress` |
| カテゴリ名・説明の変更 | `data.js` | `SITE_CATEGORIES` 内の対象 |
| カテゴリの新規追加 | `data.js` ＋ 新HTML | 両方を作成 |
| 画像の差し替え | `images/` + `data.js` | ファイル配置 ＋ パス変更 |
| お知らせ文の変更 | `main.js` | `renderHome` 内の `notice-section` |
| サイトデザインの変更 | `style.css` | 該当するCSSクラス |
